# 号商实例 .env 示例(真实值在 /opt/sub2api-vendor/.env,权限 600,不入库)
VENDOR_ADMIN_EMAIL=vendor@example.com
VENDOR_ADMIN_PASSWORD=<强口令>
DATABASE_PASSWORD=<s2vendor 角色口令>
JWT_SECRET=<openssl rand -hex 32>
TOTP_ENCRYPTION_KEY=<openssl rand -hex 32>
