# Sub2API 本地二次开发记录

## 1. 项目目标

本项目用于在本地搭建 Sub2API，逐步形成自己的本地账号池，并通过统一的 API 入口管理上游账号、代理、出口 IP、TLS Profile 和请求路由。

当前确定的架构方向：

```text
本地 Codex / Cockpit Tools
          |
          v
      Sub2API
          |
          +--> 账号池中的上游账号
          +--> 账号绑定的代理和出口 IP
          +--> 账号绑定的 TLS Profile
          +--> 上游服务
```

AIClient2API 已弃用，不再作为本项目的运行组件或后续架构依赖。

## 2. 当前运行状态

| 项目 | 当前值 | 说明 |
| --- | --- | --- |
| 前端地址 | `http://localhost:3001/` | Vite 开发服务器，当前可打开查看页面 |
| 前端端口 | `3001` | 已固定 |
| 后端目标 | `http://localhost:8080` | 前端开发代理指向这里 |
| 后端状态 | 未启动 | 当前机器没有 Go、Docker 或 Apple `container` |
| 当前分支 | `codex/tls-fingerprint-stability` | 前后端后续应使用同一分支 |
| 第一阶段提交 | `45db94f` | `fix: stabilize TLS fingerprint profile assignment and client reuse` |

前端可以正常打开，但在后端启动之前，登录、账号管理、代理管理、Profile 管理和请求测试不能正常使用。

## 3. 管理员登录说明

Sub2API 没有可以直接通用的固定管理员密码。管理员账号需要在首次初始化时创建，或者由 Docker/自动初始化流程创建。

当前代码和部署模板中的默认邮箱示例是：

```text
admin@sub2api.local
```

当 `ADMIN_PASSWORD` 留空时，首次启动流程会生成密码，并在应用日志中显示。密码不应写入 Git、本文档或永久记忆。

本机当前没有发现已启动的后端、Docker 容器、部署数据目录、数据库文件或日志，因此目前不能确认已有可登录的真实账号。启动后端后，应优先访问：

```text
http://localhost:8080
```

如果进入 setup 向导，在向导中创建自己的管理员邮箱和密码；如果使用 Docker 自动初始化，则查看 Sub2API 容器日志中的管理员密码。

## 4. 第一阶段：TLS Profile 稳定性优化（已完成）

第一阶段的目标是让账号池中的每个账号拥有稳定、可追踪的出站 TLS 配置，避免同一账号在请求过程中随机变化，也避免修改 Profile 后继续复用旧连接。

### 4.1 已修改的模块

- `backend/internal/pkg/tlsfingerprint/dialer.go`
- `backend/internal/service/tls_fingerprint_profile_service.go`
- `backend/internal/repository/http_upstream.go`
- `frontend/src/components/account/CreateAccountModal.vue`
- `frontend/src/components/account/EditAccountModal.vue`
- `docs/TLS_FINGERPRINT_CN.md`

### 4.2 已实现的行为

1. TLS Profile 的内容摘要加入客户端缓存键。
2. Profile 修改后创建新的 Transport，避免继续复用旧 TLS/HTTP2 连接。
3. 原本的随机 Profile 分配改为按账号稳定分配。
4. 前端说明改为“按账号稳定分配”，避免用户误以为每次请求都会随机切换。
5. 默认内置 Profile 采用 Node.js 24.x-oriented 配置方向。
6. 同一个账号建议固定使用一个代理、一个出口 IP 和一个 TLS Profile。
7. Profile 的变化不会被解释为官方授权、订阅资格或风控通过保证。

### 4.3 使用建议

账号池中的每个账号建议维护以下绑定关系：

```text
账号 A -> 代理 A -> 出口 IP A -> TLS Profile A
账号 B -> 代理 B -> 出口 IP B -> TLS Profile B
```

不要在短时间内频繁改变同一账号的代理、出口 IP 或 TLS Profile。指纹配置的作用是保持客户端网络特征的一致性，不是解决账号授权、额度、订阅或服务商策略问题。

## 5. 第二阶段方案：版本与能力兼容诊断层（待确认，尚未实施）

第二阶段暂时只做方案记录，未修改代码。实施前需要用户确认。

### 5.1 目标

让小白能够看懂“账号、代理、TLS Profile、客户端版本之间是否匹配”，并在出现连接问题时知道从哪里排查，而不是只看到一个笼统的失败状态。

### 5.2 Profile 元数据

为 TLS Profile 增加清晰的能力元数据，例如：

- 面向的客户端类型：Node.js、浏览器、其他客户端。
- 面向的版本范围：例如 Node.js 20.x、Node.js 24.x。
- TLS 版本范围。
- ALPN 偏好：HTTP/2、HTTP/1.1。
- Profile 内容摘要。
- 创建时间和最后修改时间。

这些字段用于诊断和展示，不应伪造为服务商认可的客户端版本证明。

### 5.3 账号页面增加诊断入口

在账号列表或账号编辑页增加“诊断连接”功能，检查并记录：

- 当前代理出口 IP。
- DNS 是否经过预期的代理路径。
- TLS 握手是否成功。
- 协商出的 TLS 版本。
- ALPN 协商结果。
- 最终使用 HTTP/1.1 还是 HTTP/2。
- 当前账号绑定的 Profile 摘要。
- 最近一次失败原因和诊断时间。

诊断结果只用于本地排错，不代表账号授权有效，也不代表一定能通过上游服务的安全策略。

### 5.4 兼容性提示

当发现明显不匹配时，前端给出警告，但默认不强制阻断。例如：

- Profile 声明面向 Node.js 24.x，但账号配置要求 HTTP/1.1 专用路径。
- 代理不支持 CONNECT 或 HTTP/2，但 Profile 强烈偏好 HTTP/2。
- TLS 握手成功，但 ALPN 协商结果与预期不同。
- 修改了账号代理，但仍沿用旧的连接状态。

提示应说明“可能导致什么问题”和“建议如何检查”，而不是只显示一个红色错误。

### 5.5 配置变更保护

修改账号代理、出口 IP 或 TLS Profile 时：

1. 显示风险提示。
2. 记录修改时间和修改后的摘要。
3. 使旧 Transport 失效并创建新 Transport。
4. 提供一次连接测试。
5. 保留最近一次成功和失败诊断结果。

### 5.6 备份和迁移

增加本地配置导出和备份能力，至少覆盖：

- 账号基本信息。
- 代理配置。
- TLS Profile 配置。
- 分组和路由关系。
- 诊断结果摘要。

导出文件不能包含明文密码、OAuth Token、API Key 或代理密码，或者必须提供明确的加密导出方式。

### 5.7 第二阶段实施顺序

建议按以下顺序实施：

1. 先增加 Profile 元数据和摘要展示。
2. 再增加账号页面的兼容性提示。
3. 再增加只读连接诊断接口。
4. 再增加“测试连接”按钮和失败原因记录。
5. 最后增加加密备份与恢复。

每一步都应配套单元测试和最小化的前端操作验证，避免一次改动过大。

## 6. Codex 客户端版本兼容说明

这是两个不同层面的版本，初学时容易混在一起：

| 版本对象 | 实际控制什么 |
| --- | --- |
| 本地 Codex 客户端版本 | Codex 自己如何发起请求、使用什么客户端能力 |
| Sub2API 前端/后端版本 | 管理页面和服务端 API 是否匹配 |
| Sub2API TLS Profile 版本 | Sub2API 访问上游时发送的 TLS ClientHello 特征 |

本地 Codex 客户端版本与 Sub2API 绑定的 TLS Profile 版本通常不会直接冲突。真正需要注意的是：

- Sub2API 前端和后端尽量使用同一分支、同一提交。
- 前后端版本不一致时，可能出现 API 字段不一致、登录异常或页面操作失败。
- TLS Profile 的 Node.js 20.x / 24.x 差异，主要是出站 TLS 特征差异，不是本地程序版本冲突。
- Cipher Suites、Supported Versions、ALPN 等组合不兼容时，可能出现 TLS 握手失败或 HTTP/2 不稳定。
- 当前不确定时，先使用默认 Node.js 24.x-oriented Profile，不要频繁切换。

## 7. 缓存边界

官方客户端、上游服务和中转层的缓存不是同一件事：

- 官方客户端是否命中 Prompt Cache，由官方协议、请求结构、模型和服务端策略决定。
- Sub2API 可以尽量保持请求结构、连接复用和账号会话稳定，但不能仅凭中转层保证固定“5 分钟缓存”。
- AIClient2API 即使有自己的缓存，也不能据此推导官方服务一定会命中同样的缓存。
- 后续测试应分别记录：请求是否命中上游缓存、Sub2API 是否复用了本地 Transport、代理连接是否复用。

因此，项目中不要把“有连接缓存”直接写成“官方一定有 5 分钟 Prompt Cache”。

## 8. 本地部署顺序

当前推荐的本地顺序：

1. 安装并确认 PostgreSQL 15+、Redis、Go 或 Docker/Apple `container`。
2. 准备 `deploy/.env`，设置数据库密码、JWT Secret、TOTP 加密密钥和管理员密码。
3. 启动后端并确认 `http://localhost:8080/health` 返回正常。
4. 保持前端开发服务运行在 `http://localhost:3001/`。
5. 打开前端，完成管理员登录。
6. 先添加一个测试账号、一个代理和一个 TLS Profile。
7. 通过测试请求验证账号、代理、Profile 和路由关系。
8. 确认稳定后，再逐步加入更多账号。

长期运行前必须：

- 修改数据库默认密码。
- 设置固定的 `JWT_SECRET`。
- 设置固定的 `TOTP_ENCRYPTION_KEY`。
- 管理员密码使用独立强密码。
- 不把 `.env`、Token、API Key、OAuth 凭据和代理密码提交到 Git。

## 9. 当前结论

- AIClient2API 已移除，不再参与架构。
- Sub2API 前端端口固定为 `3001`。
- Sub2API 后端目标端口固定为 `8080`。
- 第一阶段 TLS 稳定性优化已完成并推送到 `codex/tls-fingerprint-stability`。
- 第二阶段“版本与能力兼容诊断层”只完成方案，尚未实施。
- 当前还没有可确认的管理员登录密码；后端启动后通过 setup 向导创建，或从首次启动日志获取自动生成密码。
