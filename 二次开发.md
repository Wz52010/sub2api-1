# Sub2API 本地二次开发记录

## 1. 项目目标

本项目用于在本地搭建 Sub2API，逐步形成自己的本地账号池，并通过统一的 API 入口管理上游账号、代理、出口 IP、TLS Profile 和请求路由。

当前确定的架构方向：

```text
本地 Codex / Cockpit Tools
          |
          v
      Sub2API
          |
          +--> 账号池中的上游账号
          +--> 账号绑定的代理和出口 IP
          +--> 账号绑定的 TLS Profile
          +--> 上游服务
```

AIClient2API 已弃用，不再作为本项目的运行组件或后续架构依赖。

## 2. 当前运行状态

| 项目 | 当前值 | 说明 |
| --- | --- | --- |
| 前端地址 | `http://localhost:3001/` | Vite 开发服务器，当前可打开查看页面 |
| 前端端口 | `3001` | 已固定 |
| 后端目标 | `http://localhost:8080` | 前端开发代理指向这里 |
| 后端状态 | 暂停部署 | M2 本地 Docker 虚拟机暂不承担构建；计划在服务器启动 PostgreSQL、Redis 和后端 |
| 当前分支 | `codex/tls-fingerprint-stability` | 前后端后续应使用同一分支 |
| 第一阶段提交 | `45db94f` | `fix: stabilize TLS fingerprint profile assignment and client reuse` |

前端可以正常打开，但在后端启动之前，登录、账号管理、代理管理、Profile 管理和请求测试不能正常使用。

## 3. 管理员登录说明

Sub2API 没有可以直接通用的固定管理员密码。管理员账号需要在首次初始化时创建，或者由 Docker/自动初始化流程创建。

当前本地环境使用的管理员邮箱是：

```text
admin@sub2api.local
```

管理员密码已经写入本地 `deploy/.env`，该文件被 Git 忽略且权限已限制为当前用户可读。密码不写入 Git、本文档或永久记忆。

注意：本机后端和数据库还没有启动，因此密码现在只是完成了本地初始化配置，尚未产生可以登录的管理员记录。首次 setup/初始化完成后，用上面的邮箱和本地 `.env` 中的密码登录。

本机当前没有发现已启动的后端、Docker 容器、数据库文件或日志，因此目前不能确认已有可登录的真实账号。启动后端后，应优先访问：

```text
http://localhost:8080
```

如果进入 setup 向导，在向导中创建自己的管理员邮箱和密码；如果使用 Docker 自动初始化，则查看 Sub2API 容器日志中的管理员密码。

## 4. 第一阶段：TLS Profile 稳定性优化（已完成）

第一阶段的目标是让账号池中的每个账号拥有稳定、可追踪的出站 TLS 配置，避免同一账号在请求过程中随机变化，也避免修改 Profile 后继续复用旧连接。

### 4.1 已修改的模块

- `backend/internal/pkg/tlsfingerprint/dialer.go`
- `backend/internal/service/tls_fingerprint_profile_service.go`
- `backend/internal/repository/http_upstream.go`
- `frontend/src/components/account/CreateAccountModal.vue`
- `frontend/src/components/account/EditAccountModal.vue`
- `docs/TLS_FINGERPRINT_CN.md`

### 4.2 已实现的行为

1. TLS Profile 的内容摘要加入客户端缓存键。
2. Profile 修改后创建新的 Transport，避免继续复用旧 TLS/HTTP2 连接。
3. 原本的随机 Profile 分配改为按账号稳定分配。
4. 前端说明改为“按账号稳定分配”，避免用户误以为每次请求都会随机切换。
5. 默认内置 Profile 采用 Node.js 24.x-oriented 配置方向。
6. 同一个账号建议固定使用一个代理、一个出口 IP 和一个 TLS Profile。
7. Profile 的变化不会被解释为官方授权、订阅资格或风控通过保证。

### 4.3 使用建议

账号池中的每个账号建议维护以下绑定关系：

```text
账号 A -> 代理 A -> 出口 IP A -> TLS Profile A
账号 B -> 代理 B -> 出口 IP B -> TLS Profile B
```

不要在短时间内频繁改变同一账号的代理、出口 IP 或 TLS Profile。指纹配置的作用是保持客户端网络特征的一致性，不是解决账号授权、额度、订阅或服务商策略问题。

## 5. 第二阶段：版本与能力兼容诊断层（进行中）

第二阶段已经开始实施。目前已完成不依赖数据库迁移的基础层和连接诊断；备份恢复仍待后续实施。

### 5.1 目标

让小白能够看懂“账号、代理、TLS Profile、客户端版本之间是否匹配”，并在出现连接问题时知道从哪里排查，而不是只看到一个笼统的失败状态。

### 5.2 Profile 元数据

为 TLS Profile 增加清晰的能力元数据，例如：

- 面向的客户端类型：Node.js、浏览器、其他客户端。
- 面向的版本范围：例如 Node.js 20.x、Node.js 24.x。
- TLS 版本范围。
- ALPN 偏好：HTTP/2、HTTP/1.1。
- Profile 内容摘要。
- 创建时间和最后修改时间。

当前实现会根据现有 Profile 名称、描述和 ClientHello 字段计算并返回以下展示元数据：

- 客户端类型。
- 客户端版本范围（无法可靠推断时显示 `Unspecified`）。
- TLS 版本范围。
- ALPN 偏好。
- 不包含敏感信息的 64 位十六进制指纹摘要。

这些字段用于诊断和展示，不应伪造为服务商认可的客户端版本证明。Profile 名称中的版本只作为提示，不是官方版本认证。

### 5.3 账号页面增加诊断入口

已完成第一版兼容性提示：在账号创建/编辑页选择具体 Profile 后，会显示客户端类型、版本范围、TLS 版本和 ALPN 偏好；选择“按账号稳定分配”时，会提示其稳定选择逻辑。

已增加只读出站连接诊断接口和账号测试窗口入口。后端接口为：

```text
POST /api/v1/admin/accounts/:id/diagnose-connection
```

最近一次结果可通过以下只读接口读取，不会重新请求上游：

```text
GET /api/v1/admin/accounts/:id/connection-diagnostic
```

诊断请求不会读取或发送账号 Token、API Key、Cookie，也不会发送模型请求。它只使用账号绑定的代理和 TLS Profile 发起无凭证探测，检查：

诊断结果会检查并记录：

- 当前代理出口 IP。
- DNS 是否经过预期的代理路径。
- TLS 握手是否成功。
- 协商出的 TLS 版本。
- ALPN 协商结果。
- 最终使用 HTTP/1.1 还是 HTTP/2。
- 当前账号绑定的 Profile 摘要。
- 最近一次失败原因和诊断时间。

失败阶段会单独标记为 DNS、代理/TCP、TLS、超时或 HTTP 请求，避免把代理不通、TLS 握手失败和上游返回错误混成同一种故障。

目标请求即使返回 `401`、`403` 或 `404`，只要 HTTP 响应确实到达目标，也会标记为“网络路径已到达”。如果目标请求失败，系统仍会尝试探测出口 IP，以帮助区分“目标站点/TLS 问题”和“代理完全不可用”。出口 IP 探测使用 `api.ipify.org`。最近一次结果保存到账号 `extra.connection_diagnostic`，只保存脱敏诊断摘要，不写入账号凭据；保存失败不会让本次网络诊断变成失败。

DNS 状态仅表示本机 DNS 查询结果；启用代理时，它不能证明目标域名最终由本机还是代理端解析。自定义上游地址仍会经过现有 URL 校验策略。

诊断结果只用于本地排错，不代表账号授权有效，也不代表一定能通过上游服务的安全策略。

### 5.4 兼容性提示

当发现明显不匹配时，前端给出警告，但默认不强制阻断。例如：

- Profile 声明面向 Node.js 24.x，但账号配置要求 HTTP/1.1 专用路径。
- 代理不支持 CONNECT 或 HTTP/2，但 Profile 强烈偏好 HTTP/2。
- TLS 握手成功，但 ALPN 协商结果与预期不同。
- 修改了账号代理，但仍沿用旧的连接状态。

提示应说明“可能导致什么问题”和“建议如何检查”，而不是只显示一个红色错误。

### 5.5 配置变更保护

修改账号代理、出口 IP 或 TLS Profile 时：

1. 显示风险提示。
2. 记录修改时间和修改后的摘要。
3. 使旧 Transport 失效并创建新 Transport。
4. 提供一次连接测试。
5. 保留最近一次成功和失败诊断结果。

### 5.6 备份和迁移

增加本地配置导出和备份能力，至少覆盖：

- 账号基本信息。
- 代理配置。
- TLS Profile 配置。
- 分组和路由关系。
- 诊断结果摘要。

导出文件不能包含明文密码、OAuth Token、API Key 或代理密码，或者必须提供明确的加密导出方式。

### 5.7 第二阶段实施顺序

建议按以下顺序实施：

1. 先增加 Profile 元数据和摘要展示。
2. 再增加账号页面的兼容性提示。
3. 再增加只读连接诊断接口。
4. 再增加“测试连接”按钮、最近结果读取和失败原因记录。
5. 再增加代理/Profile 变更审计和 Transport 健康观测。（已完成）
6. 最后增加加密备份与恢复。

每一步都应配套单元测试和最小化的前端操作验证，避免一次改动过大。

### 5.8 当前实现边界

- 已完成：Profile 能力元数据和摘要计算、Profile 管理表展示、账号创建/编辑页兼容性提示、只读出站诊断接口、代理出口 IP/DNS/TLS/ALPN 实测、最近一次脱敏诊断结果保存与读取、代理/Profile 绑定变化审计、诊断结果保留保护、按账号的 Transport/HTTP2 健康观测、模型层/诊断/审计/健康观测单元测试。
- 待实现：加密备份与恢复。
- 暂不做：把提示当成“官方客户端认证”或用 TLS 指纹保证账号授权、额度和风控结果。

### 5.9 接下来的优化方向（部署前后分开）

为了避免在 M2 本地反复启动虚拟机，后续按“代码优化”和“服务器验证”分开推进：

1. 账号绑定变更审计：已完成代理、TLS 指纹开关和 TLS Profile ID 的变更前后摘要；现有审计中间件自动补充账号路径参数、操作者和时间，且不记录凭据原文。出口 IP 由代理诊断结果提供，不从代理密码或凭据推导。
2. Transport 健康观测：已完成。按账号记录请求成功/失败、Transport 条目复用/创建、TLS 握手、HTTP/2 成功/兼容性回退、代理 CONNECT、DNS/网络和超时摘要；接口为 `GET /api/v1/admin/accounts/:id/transport-health`，仅返回脱敏摘要，数据保存在进程内，重启后清零。
3. 失败分类与保护：继续完善 DNS、代理 CONNECT、TLS、HTTP 4xx/5xx、超时和上游业务错误的分层展示，避免把业务拒绝误判成指纹问题。账号池切换沿用 Sub2API 现有的 `UpstreamFailoverError`、失败账号排除、粘性会话和配额调度，不新增第二套选号器。
4. 安全备份恢复：默认脱敏导出；只有用户主动提供加密口令时才允许导出和恢复敏感凭据。
5. 服务器部署验证：启动 PostgreSQL、Redis 和 Sub2API，确认迁移、登录、Profile 管理、诊断接口、Transport 健康接口和前端页面。
6. 小规模真实测试：先用 1 个账号 + 1 个代理 + 1 个 Profile，验证连接稳定后再扩充号池；不通过批量请求测试“风控效果”。

### 5.10 连接身份摘要（已开始）

连接身份用于判断账号当前是否仍在使用同一组有效连接参数，不是官方客户端认证，也不用于规避上游安全策略。摘要由账号 ID、目标主机、脱敏代理作用域、有效 TLS Profile 内容摘要、协议模式和连接池参数生成。

当前已实现：

- TLS Profile 内容摘要继续作为 Transport 缓存键的一部分；Profile 改名不会误使连接池失效，Profile 内容改变会使旧 Transport 失效。
- 代理缓存键不再保存代理用户名或密码原文；不同代理认证仍保持 Transport 隔离。
- Transport 健康接口返回当前连接身份摘要、目标主机和身份代次，帮助确认代理/Profile/协议变更是否已经生效。
- 连接身份变更只记录脱敏摘要和时间，不记录 Token、Cookie、API Key、完整代理 URL 或代理密码。
- 账号诊断页会显示代理 ID、无凭据的代理端点（协议、主机、端口）和实际出口 IP，便于核对绑定；代理用户名和密码不进入诊断快照、日志或错误文本。

“模拟真实客户请求”的工程含义限定为请求协议兼容、请求头和请求体语义一致、连接复用稳定、失败可诊断；不能把 TLS Profile 当成防封保证。账号是否可用仍由账号本身的授权、额度和上游服务策略决定。

### 5.11 默认 TLS Profile 目录（已加入迁移 196）

首次启动包含迁移 196 的版本后，后台“TLS 指纹模板”会自动出现以下可编辑模板。模板按客户端运行时和操作系统区分；Claude 的 Opus、Sonnet、Haiku 等模型，以及 Codex 的不同模型，不需要各建一套 TLS Profile。

| 模板 | 适用运行时 | 模型范围 | 说明 |
| --- | --- | --- | --- |
| `Claude Code - Node.js 24.x` | Node.js 24.x | Claude Code 全部模型 | 当前默认基线，字段完整可编辑 |
| `Codex CLI - Node.js 24.x` | Node.js 24.x | Codex CLI 全部模型 | 与模型选择解耦，字段完整可编辑 |
| `Claude/Codex shared - Node.js 22.17.1 Linux x64` | Node.js 22.17.1 / Linux x64 | Claude Code、Codex CLI 全部模型 | Linux x64 兼容基线 |
| `Claude/Codex shared - Node.js 24.3.0 macOS arm64` | Node.js 24.3.0 / macOS arm64 | Claude Code、Codex CLI 全部模型 | Apple Silicon 兼容基线 |

模板字段包括 Cipher Suites、Curves、Point Formats、Signature Algorithms、ALPN、Supported Versions、Key Share、PSK Modes 和 Extensions。模板描述中的“全部模型”是管理标签，不会改写模型名、请求 Token 或上游权限。

### 5.12 AI 修改与发布流程

现有模板编辑器支持把 AI 输出的 YAML 粘贴到“Paste YAML”区域，解析后进入普通保存流程：

1. 先复制当前模板字段，向 AI 说明目标运行时、操作系统、改动原因和需要保留的字段。
2. 要求 AI 只返回本项目字段格式，不要返回 Token、Cookie、API Key、代理密码或账号凭据。
3. 将 YAML 粘贴到模板编辑器，点击“解析 YAML”。
4. 人工检查 Profile 名称、运行时、ALPN、TLS 版本和扩展顺序，再点击“创建/更新”。
5. 修改后先对一个测试账号执行连接诊断，确认 TLS 握手、ALPN 和 Transport 健康状态，再扩大绑定范围。

发布是数据库更新并会使旧 Transport 缓存失效；建议先复制旧模板保留回滚版本，不要直接覆盖唯一可用模板。AI 只能生成或修改配置草稿，不能凭模型名称推断真实 TLS 数据；真实字段应来自明确的运行时采集或兼容性测试。

## 6. Codex 客户端版本兼容说明

这是两个不同层面的版本，初学时容易混在一起：

| 版本对象 | 实际控制什么 |
| --- | --- |
| 本地 Codex 客户端版本 | Codex 自己如何发起请求、使用什么客户端能力 |
| Sub2API 前端/后端版本 | 管理页面和服务端 API 是否匹配 |
| Sub2API TLS Profile 版本 | Sub2API 访问上游时发送的 TLS ClientHello 特征 |

本地 Codex 客户端版本与 Sub2API 绑定的 TLS Profile 版本通常不会直接冲突。真正需要注意的是：

- Sub2API 前端和后端尽量使用同一分支、同一提交。
- 前后端版本不一致时，可能出现 API 字段不一致、登录异常或页面操作失败。
- TLS Profile 的 Node.js 20.x / 24.x 差异，主要是出站 TLS 特征差异，不是本地程序版本冲突。
- Cipher Suites、Supported Versions、ALPN 等组合不兼容时，可能出现 TLS 握手失败或 HTTP/2 不稳定。
- 当前不确定时，先使用默认 Node.js 24.x-oriented Profile，不要频繁切换。

## 7. 缓存边界

官方客户端、上游服务和中转层的缓存不是同一件事：

- 官方客户端是否命中 Prompt Cache，由官方协议、请求结构、模型和服务端策略决定。
- Sub2API 可以尽量保持请求结构、连接复用和账号会话稳定，但不能仅凭中转层保证固定“5 分钟缓存”。
- AIClient2API 即使有自己的缓存，也不能据此推导官方服务一定会命中同样的缓存。
- 后续测试应分别记录：请求是否命中上游缓存、Sub2API 是否复用了本地 Transport、代理连接是否复用。

因此，项目中不要把“有连接缓存”直接写成“官方一定有 5 分钟 Prompt Cache”。

## 8. 本地部署顺序

当前推荐的本地顺序：

1. 安装并确认 PostgreSQL 15+、Redis、Go 或 Docker/Apple `container`。
2. 准备 `deploy/.env`，设置数据库密码、JWT Secret、TOTP 加密密钥和管理员密码。
3. 启动后端并确认 `http://localhost:8080/health` 返回正常。
4. 保持前端开发服务运行在 `http://localhost:3001/`。
5. 打开前端，完成管理员登录。
6. 先添加一个测试账号、一个代理和一个 TLS Profile。
7. 通过测试请求验证账号、代理、Profile 和路由关系。
8. 确认稳定后，再逐步加入更多账号。

长期运行前必须：

- 修改数据库默认密码。
- 设置固定的 `JWT_SECRET`。
- 设置固定的 `TOTP_ENCRYPTION_KEY`。
- 管理员密码使用独立强密码。
- 不把 `.env`、Token、API Key、OAuth 凭据和代理密码提交到 Git。

## 9. 当前结论

- AIClient2API 已移除，不再参与架构。
- Sub2API 前端端口固定为 `3001`。
- Sub2API 后端目标端口固定为 `8080`。
- 第一阶段 TLS 稳定性优化已完成并推送到 `codex/tls-fingerprint-stability`。
- 第二阶段已完成 Profile 元数据、前端兼容性提示、只读连接诊断、最近一次脱敏诊断结果持久化、代理/Profile 绑定变更审计和按账号 Transport/HTTP2 健康观测；下一项是加密备份与恢复。
- 管理员密码已写入本地 `deploy/.env`，但必须等后端、PostgreSQL 和 Redis 启动并完成 setup 后才能登录验证。
